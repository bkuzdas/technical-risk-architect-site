# Cloudflare Pages configuration
# Deploy via: GitHub repo → Cloudflare Pages → Build command: npm run build, Output: dist

[build]
  command = "npm run build"
  output_directory = "dist"

[build.environment]
  NODE_VERSION = "20"
